语义约束：DTD、Schema(一种约束xml的规则)

xml的竞争：
	Annotation
	JSON轻量级的数据交换格式

xml文档结构：
	1.有且仅有一个根元素
	2.元素必须合理结束
	3.元素必须合理嵌套
	4.元素的属性必须有值
xml声明：
	1.如果使用文档声明，则必须放在文档第一行
	2.

编码：将文件中的字符转换为二进制码
解码：将二进制码翻转为字符（编码与解码的格式必须相同才可以）

xml中的空格都会被保留

使用实体引用：
&lt;	<
&gt;	>
&amp;	&
&apos;	'
&quot	"

CDATA标记
<![CDATA[******]]>其中的内容都被当做简单的字符
注释：<!--  -->	

内部DTD：
紧跟在xml声明与处理指令之后
以<!DOCTYPE 开始
以]>结束

	<? xml version="1.0" encoding="GBK" standalone="yes" ?>
	<!DOCTYPE 根元素名 [
		元素描述
	]>
	xml文档主题部分	
外部DTD：
<!DOCTYPE 根元素名 
SYSTEM ""
>
公用DTD

Schema
<xs:schema xmlns:xs="URL">
...
<xs:schema>

xml命名空间xmlns





















