javaScript是解释型语言，边解释边执行。是弱数据类型语言。	功能：		提供用户交互		动态更改内容		验证数据	特点：		脚本语言		基于对象		简单性		动态性		跨平台性	组成：		核心（ECMAScript）		文档对象模型（Document Object Mode）		浏览器对象模型	嵌入方式：		直接嵌入			<script language="javascript"></script>			可以放在head与body中。		外部引入			<script src="js/test.ja"></script>	基本语法：		注释			//			/*  */		语句			js语句是给浏览器发生命令			浏览器按顺序执行			代码块		变量			以字母，下划线开头			可以包含数字			区分大小写 			通过var来声明			变量作用域		数据类型			Number		Nubmer(a1)+Nubmer(a2)			Boolean			String			null			undefined		运算符			算数运算符			+			-			*			/			++			--			-			比较运算符			==			！=			>			>=			<			<=			===			逻辑运算符			&&			||			!	三种提示技术		confirm(message)		alert(message)		prompt(message,default)	控制语句		if 		for		while		dowhile		for in	函数		eval计算字符串表达式的值		isNaN 是不是非数字	三类对象		浏览器对象			window				document				history				location				name				status				screen			document			url		脚本对象			String			Data			Math		HTML对象			标签			<p><img><a>	DOM模型		DOM表示被加载到浏览器窗口里的当前页面		节点			DOM是节点构成的集合，此时的节点文档树上的树枝或者树叶。		分类			元素节点			属性节点			文本节点		方法			getElementById(id)			getElementByTagName(tagname)			getAttribute()			setAttribute()		属性			ChildNodes			nodeName返回元素的节点名，返回大写			nodeType节点类型：元素1 属性2 文本3			nodeValue存取文本节点值			fristChild   node.fristChild=node.childNodes[0]			lastChild			parentnode返回父节点			nextsibling返回下一个兄弟节点	事件		文本框			事件				onBlur文本框失去焦点				onChange文本框的值被修改				onFocus光标进入文本框中			方法				focus()获得焦点，即获得鼠标光标				select()选中文本内容，突出显示输入区域			属性				readonly只读，文本框中的内容不能修改		按钮			onsubmit			onclick						